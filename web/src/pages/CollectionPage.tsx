import React, { useState } from 'react';
import { Collections } from '../assets/types';

export const CollectionPage: React.FC = () => {
    const [collectionList, setCollectionList] = useState<Collections>();

    return (
        <div>

        </div>
    );
}
